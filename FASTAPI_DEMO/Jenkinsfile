pipeline {
  agent {
    kubernetes {
      label 'kaniko-agent'
      defaultContainer 'kaniko'
      yaml """
apiVersion: v1
kind: Pod
spec:
  containers:
    - name: kaniko
      image: gcr.io/kaniko-project/executor:v1.8.0  // Specify a version
      args: ["--verbosity=debug"]  // Change verbosity to debug
      tty: true
      volumeMounts:
        - name: kaniko-secret
          mountPath: /kaniko/.docker
        - name: workspace-volume
          mountPath: /workspace
  volumes:
    - name: kaniko-secret
      secret:
        secretName: docker-config
    - name: workspace-volume
      emptyDir: {}
  restartPolicy: Never
"""
    }
  }

  environment {
    DOCKER_IMAGE = "docker.io/skundu1234/fastapi-demo:${BUILD_NUMBER}"
    GIT_REPO = "https://github.com/skundu1234/jenkins-webhook-test.git"
  }

  stages {
    stage('Clone Repo') {
      steps {
        sh '''
          git clone ${GIT_REPO} /workspace
        '''
      }
    }

    stage('Build & Push with Kaniko') {
      steps {
        container('kaniko') {
          sh 'echo "🔥 Starting Kaniko build"'
          sh '''
            /kaniko/executor \
              --context=/workspace/FASTAPI_DEMO \
              --dockerfile=/workspace/FASTAPI_DEMO/Dockerfile \
              --destination=${DOCKER_IMAGE} \
              --verbosity=debug  // Increased verbosity for debugging
          '''
        }
      }
    }
  }

  post {
    success {
      echo "✅ Image pushed to ${DOCKER_IMAGE}"
    }
    failure {
      echo "❌ Build failed. Check Kaniko logs for details."
    }
    always {
      echo "📦 Jenkins pipeline finished running Kaniko"
    }
  }
}